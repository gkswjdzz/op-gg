import { styled } from '../stitches.config';

const SummonerMostWinLossWrapper = styled('div', {});

export const SummonerMostWinLoss = () => {
  return (
    <SummonerMostWinLossWrapper>
      Summoner MostWinLoss
    </SummonerMostWinLossWrapper>
  );
};
